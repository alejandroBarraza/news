<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.News" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">false</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/textColorPrimary</item>
        <item name="colorPrimaryVariant">@color/textColorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/textColorSecondary</item>
        <item name="colorSecondaryVariant">@color/textColorSecondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/statusBarColor</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:navigationBarColor">@color/navigationBarColor</item>

    </style>
    <!-- the Toolbar -->
    <style name="Theme.News.Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:background">@color/toolbarBackground</item>
        <item name="titleTextColor">@color/toolbarTitleTextColor</item>
        <item name="subtitleTextColor">@color/toolbarSubtitleTextColor</item>
        <item name="colorControlNormal">@color/windowBackground</item>
        <item name="android:colorControlNormal">@color/windowBackground</item>
        <item name="android:editTextColor">@android:color/white</item>
        <item name="android:textColorHint">@color/windowBackground</item>
    </style>

</resources>